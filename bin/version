#!/usr/bin/env bash
set -euo pipefail

BASE_VERSION="${SLACK_CLI_BASE_VERSION:-v0.4.0}"
FORK_VERSION_SUFFIX="${SLACK_CLI_FORK_VERSION_SUFFIX:-0xble.0.2.0}"

printf '%s-%s\n' "$BASE_VERSION" "$FORK_VERSION_SUFFIX"
