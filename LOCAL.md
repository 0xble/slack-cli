# Local fork notes

- Upstream: `lox/slack-cli`
- Fork: `0xble/slack-cli`
- Maintained branch: `main`
- Install path: `~/.local/bin/slack-cli`
- Install entrypoint: `bin/upgrade`
- Runtime verification: `bin/smoke`

Current fork intent:

- keep upstream behavior intact
- add parity features incrementally against the existing local `slack` tool
- switch daily usage only after the fork covers the blocking workflows
